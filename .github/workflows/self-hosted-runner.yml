name: Run using Self Hosted Runner
on:
  workflow_dispatch:

jobs:
  Simple-Script:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Echo Hello World
        run: echo "Hello World"
  
